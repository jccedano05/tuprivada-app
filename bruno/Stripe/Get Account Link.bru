meta {
  name: Get Account Link
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/stripe/accounts/:id/link
  body: none
  auth: bearer
}

params:path {
  id: acct_xxxxxxxxxxxxx
}

auth:bearer {
  token: {{auth_token}}
}
