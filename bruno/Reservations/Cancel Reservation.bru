meta {
  name: Cancel Reservation
  type: http
  seq: 3
}

delete {
  url: {{base_url}}/api/reservations/:id
  body: none
  auth: bearer
}

params:path {
  id: 1
}

auth:bearer {
  token: {{auth_token}}
}
